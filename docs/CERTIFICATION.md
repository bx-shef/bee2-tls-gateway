# Сертификация шлюза

> Last reviewed: 2026-08-13

Чему решение должно соответствовать, чтобы им можно было **легально** ходить в
Open-banking белорусских банков и другие системы из перечня СТБ 34.101.65, кто это
подтверждает и что нас отделяет от подтверждения.

Разделы 1–6 отвечают на вопрос «как устроена дорога», раздел 7 — на вопрос
«**стоит ли по ней идти**» (короткий ответ: технически реально, но для собственной
интеграции несоразмерно — см. §7.2).

Файл заведён решением владельца 13.08.2026 (исключение из правила «новых `.md` не
заводим»): материал предназначен и для передачи наружу — юристу, в ОАЦ, в испытательную
лабораторию, банку-партнёру.

⚠ Документ — кабинетное исследование по открытым источникам, **не юридическое
заключение**. Перед действиями по сертификации — консультация ОАЦ и юриста.

## 1. Требование, с которого всё началось

Инструкция Приорбанка по подключению к Open-banking API («Инструкция по подключению
к Open-banking API согласно СПР для разработчиков», api.priorbank.by) цитирует СПР 6.02:

> «Передача сообщений между клиентом и сервером авторизации должна производиться с
> использованием протокола TLS. Реализация протокола TLS с использованием
> криптографических алгоритмов Республики Беларусь должна выполняться в соответствии
> с требованиями СТБ 34.101.65 „Информационные технологии и безопасность. Протокол
> защиты транспортного уровня (TLS)“, утвержденного постановлением Государственного
> комитета по стандартизации Республики Беларусь от 22 мая 2014 г. № 23. <…> Для всех
> коммуникаций должен использоваться TLS версии 1.2 или более поздней.»

СПР — стандарты проведения расчётов Нацбанка: СПР 6.01-2020 «Банковская деятельность.
Информационные технологии. Открытые банковские API. Регламент взаимодействия
поставщиков API и пользователей API» и СПР 6.02-1-2022 «… Платежные API. Спецификация».

Дальше в инструкции — три факта, которые определяют всю задачу:

1. **Банк требует не «TLS по СТБ», а СКЗИ**: «разработчику **обязательно требуется
   приобрести СКЗИ** (средства криптографической защиты информации) для взаимодействия
   с нашим сервером авторизации». Сервер банка — программный комплекс «Сервер TLS
   АВЕСТ» (AvTLSSrv). Клиентские СКЗИ, названные банком: **AvAuthGate** (ЗАО «АВЕСТ»)
   или компоненты комплекта абонента АВЕСТ (**AvUCK**). Формулировка «в том числе» —
   список открытый: другое СКЗИ, соответствующее СТБ 34.101.65, тоже допустимо.
2. **Продуктивный контур отделён от тестового**: вызовы с СКЗИ идут на
   `https://apibel.priorbank.by:9345`; тестовая среда (`api.priorbank.by:9344`) работает
   по обычному TLS, и методы `client_secret_basic`/`client_secret_post` разрешены
   **только** в ней.
3. **«Использование API Open-banking без криптографических алгоритмов Республики
   Беларусь не соответствует требованиям СПР 6.02»** — прямым текстом. Технически
   совместимый, но несертифицированный стек — это несоответствие регуляторному
   требованию, а не инженерная проблема.

Отсюда рамка для нашего шлюза: чтобы закрывать продуктивный контур, он должен быть
**сертифицированным СКЗИ** — или работать в связке с таковым. «Реализует те же
алгоритмы» — недостаточно.

Попутно из инструкции: при аутентификации `tls_client_auth` доверенным центром
сертификации назван **ГосСУОК** — то есть клиентские сертификаты для продуктива
получаются там (наш кейс с самого начала).

## 2. Что такое «сертифицированное СКЗИ» в Беларуси

- **Рамка** — технический регламент **ТР 2013/027/BY** «Информационные технологии.
  Средства защиты информации. Информационная безопасность» (утверждён постановлением
  Совета Министров от 15.05.2013 № 375, действует с 01.01.2014). Средства защиты
  информации, выпускаемые в обращение в РБ, подтверждают соответствие его требованиям.
- **Орган по сертификации — один**: Оперативно-аналитический центр при Президенте
  Республики Беларусь (**ОАЦ**), аккредитованный орган по сертификации средств защиты
  информации (аттестат аккредитации № BY/112 036.01 от 15.10.1997). Минск, ул. Кирова,
  49; по вопросам сертификации — (017) 309-24-48; oac.gov.by.
- **Испытания** проводит аккредитованная испытательная лаборатория. Известные по
  открытым данным: ООО «БАЙТИС» (аттестат BY/112 02.1.0.1669 — испытывала AvTunProxy),
  ОДО «ВирусБлокАда» (заявляет услуги сертификационных испытаний СЗИ на своём сайте).
  Актуальный список — у ОАЦ.
- **Сертификат** для серийно выпускаемой продукции действует **5 лет**; сведения
  вносятся в государственный реестр (публикуется на oac.gov.by). Поддержание —
  периодическая оценка (видна в сертификате AvTunProxy: «отчёт по периодической
  оценке»).
- **Отдельный вопрос — лицензия на деятельность** по технической и (или)
  криптографической защите информации (лицензирующий орган — ОАЦ; с 01.01.2023
  действует Закон «О лицензировании» от 14.10.2022 № 213-З). Нужна ли она нам как
  разработчику СКЗИ и в каком составе работ — **вопрос юристу**, до подачи заявки.

⚠ Проверено: **standartno.by** (ООО «БелПрофСерт», центр «Стандарт Качества») — это
сертификация продукции по ТР ЕАЭС и систем менеджмента ISO. СКЗИ и ТР 2013/027/BY в их
услугах нет; для нашей задачи они **не подходят**. Дорога одна — через ОАЦ.

## 3. Образец цели: сертификат AvTunProxy

Сертификат соответствия на AvTunProxy (avest.by/crypto/cert/) — образец того документа,
который нужен нам. Реквизиты:

- № **BY/112 02.01. ТР027 036.01 00424**, зарегистрирован 31.03.2022, действителен по
  30.03.2027 (те самые 5 лет);
- заявитель и изготовитель — ЗАО «АВЕСТ»; продукция — «Программное средство AvTunProxy
  (**РБ.ЮСКИ.19004-03**), серийное производство» — обозначение по ЕСПД;
- выдан на основании отчёта по периодической оценке и семи протоколов испытаний
  лаборатории ООО «БАЙТИС»;
- **приложение — контрольные характеристики файлов**: хэш (по СТБ 34.101.31) четырёх
  конкретных дистрибутивов `avtunproxy-5.10.12` (linux64, osx, win32-signed, win64-signed).

Из образца следуют три практических вывода:

1. **Сертифицируются конкретные файлы конкретной версии.** Изменился дистрибутив —
   контрольная характеристика не сойдётся; новые версии проходят через внесение
   изменений в сертификат / периодическую оценку. Для нас это означает: воспроизводимая
   сборка и жёсткие пины версий — не гигиена, а условие сертифицируемости.
2. **Продукт оформляется как изделие** — с обозначением по ЕСПД и комплектом
   программной документации, а не как репозиторий.
3. В сертификате перечислены **конкретные пункты конкретных стандартов** — объём
   испытаний. Наш ожидаемый объём будет похожим (см. §4).

## 4. Стандарты: что это и что из них применяется к нам

Требования сертификата AvTunProxy: ТР 2013/027/BY (СТБ 34.101.23-2012 раздел 8,
СТБ 34.101.27-2011 класс 2, СТБ 34.101.31-2020 пункты 7.4, 7.5, 7.6 схема 1, 7.8,
приложение В, СТБ 34.101.45-2013 пункты 6.3, 7.1, 7.2, приложение Е, СТБ 34.101.47-2017
пункт 6.2, СТБ 34.101.65-2014, СТБ 34.101.78-2019 раздел 11).

| Стандарт | Название | Кодовое имя | Роль в нашем шлюзе |
| --- | --- | --- | --- |
| СТБ 34.101.65-2014 | Протокол защиты транспортного уровня (TLS) | btls | **Ядро.** Проверяется целиком (у АВЕСТ — без оговорок по пунктам) |
| СТБ 34.101.31-2020 | Алгоритмы шифрования и контроля целостности | belt | Симметричная криптография внутри TLS: шифрование, имитозащита, хэширование. У АВЕСТ проверялись пп. 7.4, 7.5, 7.6 (схема 1), 7.8, прил. В |
| СТБ 34.101.45-2013 | Алгоритмы электронной цифровой подписи и транспорта ключа на основе эллиптических кривых | bign | Подпись/ключевой обмен в хендшейке, проверка сертификатов. У АВЕСТ — пп. 6.3, 7.1, 7.2, прил. Е (прил. Е — стандартные параметры кривых уровней 128/192/256) |
| СТБ 34.101.47-2017 | Алгоритмы генерации псевдослучайных чисел | brng | Генерация ключей, сеансовых секретов, nonce. У АВЕСТ — п. 6.2. Смежный вопрос — источник энтропии (см. issue #8) |
| СТБ 34.101.78-2019 | Профиль инфраструктуры открытых ключей | bpki | Работа с сертификатами и цепочками ГосСУОК. У АВЕСТ — раздел 11 |
| СТБ 34.101.23-2012 | Синтаксис криптографических сообщений | CMS | Подпись/шифрование **сообщений** (аналог RFC 5652). Для чистого TLS-шлюза, вероятно, вне объёма — у АВЕСТ есть, потому что AvTunProxy вырабатывает ЭЦП. ⚠ В сети встречается неверная расшифровка «форматы сертификатов» — это СТБ 34.101.19; название 34.101.23 проверено по методике испытаний МИ.10123.10.01 |
| СТБ 34.101.27-2011 (действующая редакция — 2022) | Средства криптографической защиты информации. Требования безопасности | — | Рамочные требования к СКЗИ как изделию, по **классам** (у АВЕСТ — класс 2): контроль целостности, самотестирование, обращение с ключами. Определяет, чем «изделие СКЗИ» отличается от «библиотеки с алгоритмами» |

⚠ Трактовка пунктов (что именно в 7.4 или 6.3) дана по смыслу разделов; **точную
привязку сверять по официальным текстам** при составлении заявки. Официальные тексты —
tnpa.by / shop.belgiss.by (платно); неофициальные — apmi.bsu.by (НИИ ППМИ БГУ) и
github.com/bcrypto.

## 5. Методики испытаний опубликованы — и это подарок

На сайте ОАЦ в открытом доступе лежат **методики сертификационных испытаний** (2024 г.),
разработанные НИИ прикладных проблем математики и информатики БГУ — той же командой,
что развивает bee2:

`oac.gov.by/public/content/files/files/stb/101-XX.pdf`, проверены: 101-19, 101-23,
101-31, 101-45, 101-47, **101-65** (для 101-27 и 101-78 файлов по этой схеме нет).

Разбор МИ.10165.10.01 — методики по СТБ 34.101.65 (наш главный экзамен):

- **Обязательный криптонабор**: `TLS_DHE_BIGN_WITH_BELT_CTR_MAC_HBELT`. Ещё семь —
  опциональны (`…BELT_DWP_HBELT`, варианты `DHT` и `PSK`).
- Программа может реализовывать **клиентскую часть, серверную или обе**; хотя бы один
  уровень стойкости из 128/192/256. Нам нужна клиентская (мы — клиент к банкам), но
  сертифицировать можно сразу обе.
- **Испытания — три этапа**: анализ документации → тестирование → **анализ исходных
  текстов** (не менее двух экспертов по исходникам). Открытый код процессу не мешает,
  а помогает.
- Требуемая документация: «Спецификация», «Текст программы»; возможны «Описание
  программы», «Руководство программиста» — допускается оформление по ЕСПД.
- Требования к программе: точная и полная реализация протокола с заявленными
  криптонаборами и **отсутствие недокументированных возможностей**.

Практический смысл: экзаменационные билеты известны заранее. Проверку обязательного
криптонабора можно и нужно прогонять у себя на стенде до всякой заявки.

## 6. Что мешает пройти сертификацию — разрыв между нами и сертификатом

По убыванию вероятности провалить попытку — а не по объёму работ:

1. **Источник энтропии — самое вероятное место провала.** СТБ 34.101.27 предъявляет
   требования к СКЗИ как к изделию (самотестирование, контроль целостности, обращение
   с ключами, ДСЧ), а СТБ 34.101.47 стоит отдельным пунктом в объёме испытаний.
   Лаборатория смотрит сюда в первую очередь — и здесь у нас не решение, а **заявленный
   дефект**: `bee2evp_init()` поднимает busy-loop как источник энтропии (issue #8),
   и в `README.md` он честно назван «известным ограничением», смягчённым `nice -n 19`.
   Того, что снимает вопрос ДСЧ у эксперта, в проекте пока нет.
2. **Размер объекта испытаний.** Методика требует, чтобы программа не содержала
   недокументированных возможностей. У АВЕСТ объект — сфокусированный прокси; у нас
   образ везёт полный nginx, пропатченный OpenSSL и userland Debian, и требование
   распространяется на всё это. Чем больше объект, тем дороже анализ исходных текстов
   двумя экспертами. ⚠ **Решение принимать рано**: сертифицировать минимальный объект
   (клиентская часть TLS) или образ целиком — разница в цене может быть кратной.

   ⚠ **Прецедент по этому пункту создан осознанно (13.08.2026, #18/#19).** В рантайм-образ
   добавлен `bee2cmd` — то есть объект испытаний вырос, и это ровно та статья, которая
   названа дорогой. Решение владельца: добавить, **но описать как документированную
   возможность изделия** (`README.md` § «Самотестирование криптостека»). Логика в том, что
   методика запрещает не лишние файлы, а **недокументированные возможности**; из бинаря,
   про который нигде не написано, зачем он там, сделан самотест при старте — а
   самотестирование СТБ 34.101.27 прямо требует. То есть одно требование закрыто, а второе
   не нарушено, потому что возможность описана.

   Отсюда правило на будущее: **всё, что кладётся в рантайм-образ, либо описано как
   возможность, либо не кладётся.** Молчаливое «просто пусть лежит, вдруг пригодится» —
   это и есть недокументированная возможность, и стоить она будет на анализе исходных
   текстов, а не сейчас.
3. **Сертифицируют артефакты, а мы — репозиторий.** Нужны выпущенная версия с
   публикуемыми контрольными характеристиками файлов, обозначение и заявленное
   «серийное производство».
   ⚠ Половина этого уже есть и не ради сертификации: `Dockerfile` пинует bee2evp по
   коммиту, OpenSSL по тегу, nginx по sha256, базовый образ по digest (три пина из
   четырёх не видит Dependabot — issue #5).
   ⚠ **Постоянный налог, а не разовая работа**: сертифицированный артефакт и вовремя
   пропатченный артефакт тянут в разные стороны. Вышла CVE в OpenSSL — обновились,
   хэши разъехались, и сертификат перестал описывать то, что реально работает. Для
   шлюза, который обязан следить за CVE в криптостеке, это противоречие не разовое.
4. **Комплекта программной документации нет.** «Спецификация» и «Текст программы» —
   минимум по методике; отдельная работа на месяцы, побочным эффектом разработки она
   не появится.
5. **Обязательный криптонабор заявлен, но вживую подтверждён обратный.**
   `nginx.conf.template` ставит первым `DHE-BIGN-WITH-BELT-CTR-MAC-HBELT` — это и есть
   обязательный по методике `TLS_DHE_BIGN_WITH_BELT_CTR_MAC_HBELT`, следом три
   опциональных из того же списка. Но единственный имеющийся живой замер, сделанный до
   появления явного списка, **вернулся с DHT**, а не DHE.
   ⚠ Для сертификации это не смертельно: методика требует, чтобы программа
   **реализовывала** обязательный набор, а не чтобы конкретный сервер его выбрал.
   Для безопасности — существенно: DHT это транспорт ключа без forward secrecy. Живой
   прогон с логом реально согласованного шифра нужен независимо от сертификации
   (PROCESS.md §4).
6. **bee2/bee2evp сами по себе сертификатом не являются.** Сертифицируется продукт,
   а не библиотека. Проверить реестр ОАЦ: есть ли уже сертифицированные изделия на
   этом стеке (реестр публикуется на oac.gov.by).
7. **Лицензия на деятельность** по технической и (или) криптографической защите
   информации — открытый юридический вопрос (см. §2). Может закрыть тему до её начала:
   вопрос не «сможем ли пройти испытания», а «может ли ИП вообще выступить
   изготовителем СКЗИ».
8. **Стоимость и сроки неизвестны.** Запрашивать у ОАЦ и лаборатории; по косвенным
   признакам (семь протоколов испытаний у АВЕСТ) — это месяцы, не недели.

## 7. Насколько это реально

**Технически — да, и позиция сильнее обычной. Организационно — это регуляторный проект,
а не инженерный, и он длиннее и дороже всей разработки шлюза.**

### 7.1. Что играет за нас

- **Шлюз уже отработал вживую против боевого банка.** 12.08.2026,
  `apibel.priorbank.by:9345`, `upstream=401` в логе (README.md § «Проверено вживую»).
  Значит, рукопожатие по СТБ 34.101.65 состоялось **с сертифицированным сервером**
  АВЕСТ (AvTLSSrv), цепочка ГосСУОК проверилась, имя хоста сверилось. Контрольный
  `openssl s_client` на тот же хост в тот же день упал с `unknown cipher returned`.
  Совместная работа с сертифицированной реализацией — сильнейшее свидетельство
  корректности протокола, какое вообще бывает до испытаний.
- **Криптографию писали не мы.** bee2/bee2evp — НИИ ППМИ БГУ, тот же институт написал
  и методики испытаний. Сертифицируется интеграция эталонной реализации, а не
  самодельная криптография, и экзамен сдаётся по опубликованным билетам (§5).
- **Открытость — плюс.** Третий этап испытаний — анализ исходных текстов двумя
  экспертами. Закрытому проекту это стоит нервов, нам — ничего.

### 7.2. Главный вопрос: а нужен ли сертификат именно нам

СПР 6.02 адресует требование пользователю API, и Приорбанк формулирует его как
«разработчику обязательно требуется **приобрести** СКЗИ» (§1). То есть в продуктиве
сертификат нужен — но не обязательно **наш**. Отсюда три стратегии:

| Стратегия | Что делаем | Чем платим |
| --- | --- | --- |
| **Не сертифицировать** | шлюз остаётся open source и инструментом для тестовых контуров и разработки — там сертификация не требуется вовсе | продуктив на нём закрыть нельзя |
| **Купить чужое СКЗИ на продуктив** | AvAuthGate / AvUCK у АВЕСТ, наш шлюз — дев-инструмент и понимание предмета | лицензии и зависимость от вендора |
| **Сертифицировать своё** | полный путь §6 | месяцы, деньги, постоянный налог на обновления |

**Рекомендация: третья стратегия оправдана только если шлюз становится продуктом на
продажу** — тогда сертификат и есть товар. Ради собственной интеграции она
несоразмерна: дешевле купить готовое СКЗИ, а свой шлюз держать там, где он и так
незаменим — в разработке и тестовых контурах.

### 7.3. Ход, который стоит сделать до любых расчётов

**Написать в НИИ ППМИ БГУ.** Они авторы и bee2, и методик испытаний. Вероятность, что
на bee2evp уже есть сертифицированное изделие или что путь проходится совместно с ними,
не нулевая — а проверяется одним письмом. Заодно смотрим реестр сертификатов ОАЦ.

⚠ Оценка сделана 13.08.2026 по состоянию проекта на коммит `bdb9900` и пересматривается,
когда появится живой прогон с логом шифра или ответ ОАЦ о стоимости.

## 8. Следующие шаги

1. Письмо в НИИ ППМИ БГУ: есть ли сертифицированные изделия на bee2evp, возможен ли
   совместный путь.
2. Живой прогон с логированием реально согласованного шифра — закрывает и §6 п. 5,
   и вопрос forward secrecy.
3. Запрос в ОАЦ: порядок, стоимость, сроки, нужна ли лицензия для нашего состава работ.
4. Запрос в лабораторию (БАЙТИС): предварительная оценка объёма испытаний — отдельно
   для минимального объекта и для образа целиком (§6 п. 2).
5. Проверить реестр сертификатов ОАЦ на изделия на базе bee2evp.
6. Юрист: лицензирование деятельности, форма «серийного производства» для ИП/юрлица.
7. Купить официальные тексты СТБ 34.101.65, .31, .45, .47, .78, .27 (shop.belgiss.by) —
   когда решение идти станет принятым, а не раньше.

## 9. Лицензия репозитория

Наш код — **MIT** (LICENSE, © 2026 Igor Shevchik). Состав сверен по `Dockerfile`, версии —
те, что реально пинуются в нём `ARG`-ами на момент правки этого раздела:

| Компонент | Версия в образе | Лицензия | Обязанности при распространении |
| --- | --- | --- | --- |
| bee2 + bee2evp (НИИ ППМИ БГУ) | коммит `2ae3c71e` (bee2 — подмодуль, один sha пинует весь криптостек) | Apache-2.0 ⚠ см. ниже | Приложить текст лицензии и NOTICE, перечислить изменения, если вносили |
| OpenSSL | `openssl-3.5.6` + наш патч libssl | Apache-2.0 | То же. ⚠ Патч — это и есть «изменение», его надо назвать |
| nginx | 1.28.0 (sha256 сверяется при сборке) | BSD-2-Clause | Сохранить текст лицензии и копирайт |
| Базовый образ `debian:12-slim` + `gettext-base`, `libpcre2-8-0` | пин по digest | набор лицензий Debian (GPL/LGPL и др.) | Обязанности копилефта на **бинарные** пакеты в образе: по запросу — предложение исходников |
| Наш код | — | MIT | Сохранить копирайт |

⚠ **`bee2` бывает двух видов**: собирается `agievich/bee2` (Apache-2.0), а в организации
`bcrypto`, которой принадлежит bee2evp, лежит одноимённый `bcrypto/bee2` под **GPL-3.0**.
Проверка «по имени организации» находит copyleft там, где его нет. Разбор — `PROCESS.md`
§5, атрибуция — [`NOTICE`](../NOTICE).

Выводы:

- **Совместимость полная**: MIT-обвязка поверх Apache-2.0 и BSD-компонентов — обычная
  и беспроблемная комбинация; собирать и распространять Docker-образ можно.
- **Docker-образ — это распространение**: с момента публикации образа обязанности
  Apache-2.0/BSD включаются — файл NOTICE и тексты лицензий должны ехать в образе.
  ✅ Сделано: `/usr/share/licenses/` внутри образа, тексты копируются из тех самых
  деревьев, которые собирались; сторожит гейт 4 в `Dockerfile`.
- ⚠ **Патч OpenSSL — отдельная обязанность.** Apache-2.0 (п. 4b) требует помечать
  изменённые файлы. Мы патчим libssl, значит в NOTICE это должно быть названо, а не
  подразумеваться. Для сертификации то же самое ещё важнее: испытания включают анализ
  исходных текстов, и «наш OpenSSL отличается от апстрима» эксперт увидит в любом случае —
  лучше, чтобы это было заявлено нами, а не найдено ими.
  ✅ Сделано: раздел MODIFICATIONS в `NOTICE` называет патч BTLS к libssl (авторства
  bee2evp, мы только применяем) и починку `scripts/source.sh` у bee2evp.
- ⚠ **Базовый образ тоже уезжает потребителю.** Мы распространяем не только свой код,
  но и бинарные пакеты Debian внутри образа; у части из них копилефтные лицензии.
  Обычная практика (и то, чем закрываются все, кто публикует образы на базе дистрибутива) —
  ссылка на исходники дистрибутива, а не их выкладывание.
  ✅ Сделано: в `NOTICE` названы копилефтные пакеты (glibc — LGPL-2.1+, gettext-base —
  GPL-3.0+) и дано письменное предложение исходников со ссылкой на `snapshot.debian.org`,
  который держит именно те версии, что стояли, — базовый образ пинуется по digest.
- **Сертификации открытость не мешает, а помогает**: испытания включают анализ исходных
  текстов двумя экспертами (§5). Сертификат привязан к контрольным характеристикам
  файлов, а не к правовому режиму кода.

## 10. Методики испытаний: реестр и план проверки

### 10.1. Реестр методик

ОАЦ выкладывает методики сертификационных испытаний открыто. Все — разработки
НИИ прикладных проблем математики и информатики БГУ, 2024 год, того же института,
что развивает bee2. Адрес единообразный:
`https://www.oac.gov.by/public/content/files/files/stb/<НОМЕР>.pdf`.

| Файл | Методика | Стандарт | Отношение к шлюзу |
| --- | --- | --- | --- |
| [`101-65.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-65.pdf) | МИ.10165.10.01 | СТБ 34.101.65-2014 «Протокол защиты транспортного уровня (TLS)» | **Главный экзамен** |
| [`101-31.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-31.pdf) | МИ.10131.10.01 | СТБ 34.101.31-2020 «Алгоритмы шифрования и контроля целостности» | belt: шифрование, имитозащита, хэш |
| [`101-45.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-45.pdf) | МИ.10145.10.01 | СТБ 34.101.45-2013 «Алгоритмы ЭЦП и транспорта ключа на эллиптических кривых» | bign: подпись и обмен ключом |
| [`101-47.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-47.pdf) | МИ.10147.10.01 | СТБ 34.101.47-2017 «Криптографические алгоритмы генерации псевдослучайных чисел» | ДСЧ — самое узкое место (§6 п. 1) |
| [`101-19.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-19.pdf) | МИ.10119.10.01 | СТБ 34.101.19-2012 «Форматы сертификатов и списков отозванных сертификатов» | проверка цепочек ГосСУОК |
| [`101-77.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-77.pdf) | МИ.10177.10.01 | СТБ 34.101.77-2020 «Криптографические алгоритмы на основе sponge-функции» | bash: в шлюзе не используется |
| [`101-23.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-23.pdf) | МИ.10123.10.01 | СТБ 34.101.23-2012 «Синтаксис криптографических сообщений» | вне объёма: мы не подписываем сообщения |
| [`101-66.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-66.pdf) | МИ.10166.10.01 | СТБ 34.101.66-2014 «Протоколы формирования общего ключа» | вне объёма |
| [`101-60.pdf`](https://www.oac.gov.by/public/content/files/files/stb/101-60.pdf) | МИ.10160.10.01 | СТБ 34.101.60-2014 «Алгоритмы разделения секрета» | вне объёма |

⚠ Для СТБ 34.101.27 (требования к СКЗИ) и 34.101.78 (профиль PKI) методик по этой
схеме **нет** — проверено запросом, отдаёт 404. А это ровно те два документа, которые
предъявляют требования к изделию, а не к алгоритму. Объём испытаний по ним придётся
выяснять у лаборатории.

### 10.2. Как устроены испытания

Одинаково во всех методиках — **три этапа**, и они распределены между разными людьми:

1. **Анализ документации** — эксперты по документации; нужны «Спецификация» и «Текст
   программы», допускается ЕСПД.
2. **Тестирование** — эксперты по тестированию; контрольные примеры стандартов.
3. **Анализ исходных текстов** — **не менее двух** экспертов.

Требование, повторяющееся дословно во всех методиках: алгоритмы должны быть реализованы
«точно и полно», и программа **не должна содержать недокументированных возможностей**.

### 10.3. План проверки

| # | Что проверяем | Чем закрывается | Где |
| --- | --- | --- | --- |
| 1 | Контрольные примеры belt / bign / brng / bash | `bee2cmd st alg` | **автомат: CI + каждый старт** |
| 2 | Самотестирование ДСЧ | `bee2cmd st rng` | **автомат: CI + каждый старт** |
| 3 | Здоровье источников энтропии | `bee2cmd es print` | **автомат: CI** |
| 4 | Обязательный криптонабор предлагается стеком | `openssl ciphers` | **автомат: CI** |
| 5 | Контрольная характеристика (belt-хэш) считается верно | сверка с эталоном ОАЦ | **вручную** (нужна сеть наружу) |
| 6 | Криптонабор реально согласуется с банком | живой прогон с логом шифра | **вручную** |
| 7 | Цепочка ГосСУОК проверяется | два гейта в `entrypoint.sh` | автомат (при старте) |
| 8 | Нет недокументированных возможностей | анализ исходных текстов | **лаборатория**; `bee2cmd` в рантайме описан как документированная возможность — см. §6 |
| 9 | Комплект документации | «Спецификация», «Текст программы» | **не начато** |

Пункты 1–5 выполняет [`scripts/check-crypto.sh`](../scripts/check-crypto.sh).

**С #19 пункты 1–4 гоняются в CI автоматически** — job `image` запускает скрипт ВНУТРИ
собранного образа, где `bee2cmd` теперь есть (`/opt/btls/bin/bee2cmd`). Скрипт при этом
монтируется, а не копируется в образ: он инструмент проекта, а не часть шлюза, и объект
испытаний им растить незачем.

**Пункт 5 остаётся ручным**: он скачивает дистрибутив АВЕСТ, а наружу из CI хода нет.
В контейнере он честно помечается пропущенным — не «пройден».

```bash
# внутри собранного образа bee2cmd уже есть, --build не нужен
docker run --rm --entrypoint bash -v "$PWD/scripts:/scripts:ro" \
  ghcr.io/bx-shef/bee2-tls-gateway:v1 /scripts/check-crypto.sh

# вне образа, на машине разработчика
bash scripts/check-crypto.sh --build                # собрать bee2cmd из пина и прогнать
CHECK_AVTP=1 bash scripts/check-crypto.sh --build   # плюс сверка с эталоном ОАЦ
```

⚠ **Пункты 1 и 2 стали ещё и самотестом при старте** (#18): `entrypoint.sh` прогоняет
`st alg` и `st rng` при каждом запуске контейнера и **падает**, если они не сошлись —
требование СТБ 34.101.27 к изделию. Подробности и обоснование, почему `bee2cmd` живёт в
рантайме, — `README.md` § «Самотестирование криптостека».

Вне образа скрипт собирает bee2 **на том же коммите**, который приезжает подмодулем в
запиненный bee2evp (`d9e689a0` внутри `2ae3c71e`), — иначе зелёный прогон ничего не
говорил бы об образе. Внутри образа этот вопрос снят по построению: `bee2cmd` собран из
самого подмодуля.

### 10.4. Что уже прогнано — 13.08.2026

```
st alg                          контрольные примеры сошлись
st rng                          генератор проходит самотест
es print                        trng+ trng2+ sys+ timer+, оба показателя здоровья +
belt-хэш AvTunProxy 5.10.12     40bbf258…37fb698 — совпал с сертификатом ОАЦ
```

⚠ **Пункт 5 — самая ценная из проверок, и вот почему.** Приложение к сертификату
№ BY/112 02.01. ТР027 036.01 00424 публикует контрольную характеристику дистрибутива
`avtunproxy-5.10.12.linux64.tar.gz`, вычисленную по СТБ 34.101.31. Мы скачали этот
дистрибутив, посчитали belt-хэш **своей** сборкой bee2 — и получили значение, заверенное
органом по сертификации, знак в знак. То есть наша реализация belt-хэша считает ровно то
же, что считала испытательная лаборатория. Такой эталон не выдумаешь: он подписан
государственным органом и опубликован.

⚠ Расхождение в будущем не будет означать ошибку реализации: АВЕСТ может выложить более
свежую сборку под именем `latest`. Сначала проверяется версия в файле `version` внутри
архива, и только потом делается вывод.
