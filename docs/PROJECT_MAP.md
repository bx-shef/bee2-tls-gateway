# Карта проекта

> Last reviewed: 2026-08-13

Из чего состоит проект и что в каком состоянии. Тронул подсистему — обнови её строку
**в том же PR**.

Статусы: `сделано` — написано и работает; `проверено вживую` — прогнано на стенде;
`отложено` — решение принято, срок не назначен; `блокер` — мешает двигаться дальше.

| Подсистема | Состояние | Комментарий |
| --- | --- | --- |
| Защита `main` | сделано | GitHub Ruleset импортирован 13.08.2026, состав — в `CLAUDE.md` |
| CI (job `ci`) | сделано | shellcheck, синтаксис JSON/YAML, проверки конфига и мутации к ним, штампы `Last reviewed` |
| CI (job `image`) | сделано | сборка образа (~20 мин) и смоук-запуск; первый живой прогон — PR [#10](https://github.com/bx-shef/bee2-tls-gateway/pull/10) |
| Правила ведения проекта | сделано | `CLAUDE.md`: конвенции, коммиты, ревью и мерж PR |
| Разбор окружения (AvTunProxy, хосты BTLS) | сделано, вживую не проверено | `PROCESS.md` §2: кто за хостами, где есть API, чем AvTunProxy отличается от нас |
| Сертификация как СКЗИ | изучено, не начато | `CERTIFICATION.md`: СПР 6.02, ТР 2013/027/BY, ОАЦ, методики испытаний, лицензии. Оценка §7: технически реально, для своей интеграции несоразмерно |
| Проверка по методикам ОАЦ | сделано, вручную | `scripts/check-crypto.sh` — контрольные примеры, самотест ДСЧ, здоровье источников, криптонабор, сверка belt-хэша с эталоном ОАЦ. План — `CERTIFICATION.md` §10 |
| Сборка bee2 | проверено вживую | стадия `btls` в `Dockerfile`, пин коммита `2ae3c71e` |
| Сборка OpenSSL с провайдером bee2 | проверено вживую | `openssl-3.5.6` + патч libssl, три гейта сборки |
| Сборка nginx | сделано, вживую не перепроверено | `1.30.4` (stable) — ушли с legacy 1.28 из-за CVE-2026-1642 и CVE-2026-42533; **нужен живой прогон против банка** ([#15](https://github.com/bx-shef/bee2-tls-gateway/issues/15)) |
| Конфигурация nginx (BTLS наружу, HTTP внутрь) | проверено вживую | `nginx.conf.template`, allowlist путей — единственная граница |
| Работа с сертификатами ГосСУОК | сделано | `ca/`, два стартовых гейта; следить за сроком — [#4](https://github.com/bx-shef/bee2-tls-gateway/issues/4) |
| Тесты | сделано | `scripts/check-config.sh` (намерение) + смоук в job `image` (поведение) + `scripts/check-pins.sh` (пины согласованы) + `scripts/check-config-mutations.sh` (сами проверки живы) |
| Лицензии и атрибуция | сделано | `NOTICE`, тексты апстримов в образе, гейт 4; план Б — `PROCESS.md` §5 ([#6](https://github.com/bx-shef/bee2-tls-gateway/issues/6)) |
| Развёртывание | сделано, вживую не проверено | публикация в GHCR при мерже в `main` после смоука; прод пинится по `@sha256:`, `v1` — подвижный алиас ([#14](https://github.com/bx-shef/bee2-tls-gateway/issues/14)) |

## Блокеры

- **Живой прогон против банка после смены минорной версии nginx** (1.28 → 1.30.4,
  [#15](https://github.com/bx-shef/bee2-tls-gateway/issues/15)). Сборка, гейты и смоук
  доказывают, что образ собрался и стартует; что банк по-прежнему отвечает — не доказывают.
  Прогон ручной, требует доступа по договору. До него бамп считается непроверенным.
